int main() {
    decl 
        int num, temp;
        str next;
    enddecl

    begin 
        num = 1;
        while (num <= 99) do
            temp = exposcall("Write", -2, num);
            num = num + 2;
        endwhile;

        next = "even.xsm";
        temp = exposcall("Exec", next);

        return 0;
    end
}
